spring:
  application:
    name: credito

  datasource:
    url: jdbc:mysql://localhost:3306/sicredi_credito
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
